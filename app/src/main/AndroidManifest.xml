<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groupproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Restaurant_AppTheme">
        <activity android:name=".DrinksMenuActivity"></activity>
        <activity android:name=".KidsMenuActivity"></activity>
        <activity android:name=".AdultsMenuActivity"></activity>
        <activity android:name=".DessertsMenuActivity"></activity>
        <activity android:name=".CalculateBillActivity"></activity>
        <activity android:name=".EditFoodItemActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>